# Authentication Configuration
# Set to 'false' or '0' to disable authentication
AUTH_ENABLED=true

# User credentials in format: username:password,username2:password2
# ⚠️ CHANGE THESE DEFAULTS IN PRODUCTION!
# Use strong passwords with mix of uppercase, lowercase, numbers, and symbols
AUTH_USERS=admin:your-secure-password-here

# CORS Configuration
# Set specific origins in production, e.g., https://yourdomain.com
ALLOWED_ORIGINS=*
